#!/bin/bash
# Post-removal script for simple-smtpd

set -e

PROJECT_NAME="simple-smtpd"
CONFIG_DIR="/etc/$simple-smtpd"
LOG_DIR="/var/log/$simple-smtpd"

if [ "$1" = "purge" ]; then
    # Remove configuration files (optional - user may want to keep them)
    # Uncomment to remove config on purge:
    # rm -rf "$CONFIG_DIR"
    
    # Remove log files
    rm -rf "$LOG_DIR"
    
    # Remove service user (optional)
    # userdel smtpddev 2>/dev/null || true
fi

exit 0

